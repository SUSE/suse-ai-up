apiVersion: v2
name: mcp-gateway
description: SUSE AI Universal Proxy with Proxy Support
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: mcp-proxy
    version: "1.0.0"
    condition: proxy.enabled