{
  "name": "test-virtualmcp",
  "description": "Test VirtualMCP server for create-adapter testing",
  "tools": [
    {
      "name": "test_tool",
      "description": "A simple test tool",
      "input_schema": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "description": "Message to echo"
          }
        },
        "required": ["message"]
      }
    }
  ]
}
